apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: catalog
  namespace: demo
spec:
  replicas: 2
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: catalog
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "true"
      labels:
        app: catalog
        istio: monitor
    spec:
      containers:
      - name: catalog
        image: devopsflow999/catalog:main-v0.1.2
        env:
          - name: PGUSER
            value: appuser
          - name: PGPASSWORD
            value: appuser
          - name: PGDATABASE
            value: mv100db
          - name: PGHOST
            value: postgres-postgresql.kafka.svc.cluster.local
          - name: PGPORT
            value: "5432"
          - name: REDIS_URL
            value: redis-master.kafka.svc.cluster.local:6379
          - name: KAFKA_BROKERS
            value: kafka.kafka.svc.cluster.local:9092
          - name: KAFKA_CLIENT_ID
            value: catalog-service
          - name: KAFKA_TOPIC_PRODUCTS
            value: mv100db_public_products
          - name: LOG_LEVEL
            value: info
          - name: PORT
            value: "3001"
        ports:
        - name: http
          containerPort: 3001
          protocol: TCP
        resources:
          requests:
            memory: 32Mi
            cpu: 5m
  strategy:
    canary:
      canaryService: catalog-canary
      stableService: catalog-stable
      # analysis will be performed in background, while rollout is progressing through its steps
      # analysis:
      #   startingStep: 1   # index of step list, of when to start this analysis
      #   templates:
      #   - templateName: istio-success-rate
        # args:             # arguments allow AnalysisTemplates to be re-used
        # - name: service 
        #   value: catalog-canary
        # - name: namespace
        #   valueFrom:
        #     fieldRef:
        #       fieldPath: metadata.namespace
      trafficRouting:
        istio:
          virtualService:
            name: catalog-vsvc
            routes:
            - catalog-route
      steps:
      - setWeight: 10
      - pause: {}         # pause indefinitely
      - setWeight: 20
      - pause: {duration: 20s}
      - setWeight: 30
      - pause: {duration: 20s}
      - setWeight: 40
      - pause: {duration: 20s}
      - setWeight: 50
      - pause: {duration: 20s}
      - setWeight: 60
      - pause: {duration: 20s}
      - setWeight: 70
      - pause: {duration: 20s}
      - setWeight: 80
      - pause: {duration: 20s}
      - setWeight: 90
      - pause: {duration: 20s}